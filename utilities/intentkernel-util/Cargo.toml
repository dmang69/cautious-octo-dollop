[package]
name = "intentkernel-util"
version.workspace = true
edition.workspace = true
description = "Shared utilities for IntentKernel (config, paths, time)"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
toml = "0.8"