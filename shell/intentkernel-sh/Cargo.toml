[package]
name = "intentkernel-sh"
version.workspace = true
edition.workspace = true
description = "IntentKernel interactive shell — kernel, crypto, and capability operations"

[[bin]]
name = "iksh"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
hex = "0.4"
intentkernel-core = { path = "../../kernel/intentkernel-core" }
intentkernel-crypto = { path = "../../crypto/intentkernel-crypto" }
intentkernel-util = { path = "../../utilities/intentkernel-util" }
serde = { workspace = true }
serde_json = { workspace = true }